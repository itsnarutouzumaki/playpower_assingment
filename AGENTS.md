# Airbnb Clone — Agent Instructions

## Goal
Pixel-perfect clone of https://airbnb-clone-umber-two.vercel.app
Reference is the single source of truth — match spacing, typography, colors, and interactions exactly. Do not invent design choices not present in the reference.

## Stack
React.js + Javascript + Tailwind CSS. Desktop only

## Views required
1. Listing page (hero grid, title, host info, amenities, booking card)
2. Photo tour (full grid gallery, opened via "Show all photos")
3. Lightbox (single photo, prev/next arrows, ←/→ keyboard nav, focus trap)

## Constraints
- Do not copy code directly from the reference site — rebuild from visual/behavioral reference.
- Accessibility: keyboard navigation, visible focus states, ARIA roles on interactive elements.
- Match hover/scroll animation timing and easing as closely as possible.